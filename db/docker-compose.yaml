version: '3.8'
services:
  db:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=fast
    volumes:
      - ./dump.sql:/docker-entrypoint-initdb.d/dump.sql